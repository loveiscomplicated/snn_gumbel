base: lsm_shd_alif_random_sparse_p045_a010_b005.yaml

experiment_name: lsm_shd_alif_random_sparse_p045_readout_membrane_trace

liquid:
  # Control: same fixed random topology, but read subthreshold membrane traces.
  readout_mode: membrane_trace
